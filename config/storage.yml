production:
  service: Disk
  root: <%= Rails.root.join("storage") %>

development:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>
 
test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>