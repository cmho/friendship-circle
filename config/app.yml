# config/app.yml for rails-settings-cached
defaults: &defaults
  ring_name: "Friendship Circle"
  ring_description: "Default description goes here."
  ring_rules: ""

development:
  <<: *defaults

test:
  <<: *defaults

production:
  <<: *defaults
